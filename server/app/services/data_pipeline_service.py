def process_data(file_path: str):
    # Simulate processing logic
    # Perform S3 upload, Bronze -> Silver -> Gold pipeline, etc.
    print(f"Processing file at {file_path}")
    return f"Pipeline for {file_path} started successfully."

def fetch_pipeline_status(pipeline_id: str):
    # Simulate status fetching
    # Query database or pipeline system
    print(f"Fetching status for pipeline ID {pipeline_id}")
    return "Pipeline is running"